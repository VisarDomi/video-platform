# Comma-separated list of folders where you will serve the videos from.
# An 'edited' subfolder will be created automatically in each of these directories.
VIDEOS_DIRS=/home/visar/Documents/tango,/home/visar/Videos/oldtango/converted